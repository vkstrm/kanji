# Ngo

A CLI with some tools for helping you out with Japanese.

Either search for kanji readings and meanings using the `kanji` command for transform between romaji and kana with the `kana` command.

For now run with environment variable like so`DB_PATH=path/to/sqlite.db cargo run`

